---
// GSAPLoader.astro - GSAP loader using the installed package
---

<script>
  // Import GSAP from the installed package instead of CDN
  import { gsap } from 'gsap';
  
  // Make GSAP available globally if needed
  if (typeof window !== 'undefined') {
    window.gsap = gsap;
  }
</script>