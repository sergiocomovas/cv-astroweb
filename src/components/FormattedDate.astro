---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('es-es', {
			year: 'numeric',
			month: '2-digit',
			day: '2-digit',
		})
	}
</time>
